# Copy to .env in the repository root, then generate your own secret key.
DJANGO_SECRET_KEY=replace-with-a-new-random-secret-key

# Local development only. Set False on the deployed site.
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,[::1]
